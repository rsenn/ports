# $Id: Pkgtemplate 459 2007-09-24 23:30:12Z root $
#
# Description: Synchronizes an IMAP4 mailbox with a local maildir mailbox.
#  isync is a commandline application which synchronizes a local
#  maildir-style mailbox with a remote IMAP4 mailbox, suitable for use in
#  IMAP-disconnected mode. Multiple copies of the remote IMAP4 mailbox can
#  be maintained, and all flags are synchronized. TLS/SSL is supported via
#  imaps: or STARTTLS.
#  .
# URL: http://isync.sf.net/
# Maintainer: Roman Senn <roman.l.senn@gmail.com>
# Depends on:

name=isync
version=
release=1
source=()

build()
{
  :
}

# EOF
