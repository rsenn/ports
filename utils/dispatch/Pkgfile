# $Id: Pkgtemplate 459 2007-09-24 23:30:12Z root $
#
# Description: Object-oriented system administration.
# dispatch is a multi-tier architecture object-oriented framework
# that allows access to methods and variables at the command line and
# in shell scripts or via a GUI. It contains classes that deal with
# configuration management. It tries to unify similar configuration
# semantics syntactically.
#
# URL: http://dispatch.sourceforge.net
# Maintainer: Roman Senn <rs@adfinis.com>
# Depends on:

name=dispatch
version=0.0.1.tgz
release=1
source=($mirror_sf/dispatch/dispatch-std-classes-$version)

build()
{
  :
}
