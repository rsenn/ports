diff -r -u src_orig/libtool-1.5.22/ltmain.in src/libtool-1.5.22/ltmain.in
--- src_orig/libtool-1.5.22/ltmain.in	2007-04-22 19:21:15.000000000 +0200
+++ src/libtool-1.5.22/ltmain.in	2007-04-22 19:21:49.000000000 +0200
@@ -24,6 +24,17 @@
 # configuration script generated by Autoconf, you may include it under
 # the same distribution terms that you use for the rest of that program.
 
+: ${SED:=sed}
+: ${EGREP:=egrep}
+OBJDUMP="$host-objdump"
+: ${max_cmd_len:=255}
+shlib_path="$prefix/bin $prefix/lib"
+shlibpath_var='shlib_path'
+
+if [ "$DLLTOOL" = "dlltool" ]; then
+  DLLTOOL="$host-dlltool"
+fi
+
 basename="s,^.*/,,g"
 
 # Work around backward compatibility issue on IRIX 6.5. On IRIX 6.4+, sh
